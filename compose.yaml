services:
  caddy:
    image: caddy:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - frontend

  user-service:
    build: ./user-service
    env_file:
      - ./user-service/.env

  vehicle-service:
    build: ./vehicle-service
    env_file:
      - ./vehicle-service/.env

  frontend:
    build: ./frontend
    depends_on:
      - user-service
      - vehicle-service

volumes:
  caddy_data:
  caddy_config:
